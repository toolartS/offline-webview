plugins {
    id 'com.android.application'
}

android {
    namespace "com.example.webview"
    compileSdk 34

    defaultConfig {
        applicationId "com.example.webview"
        minSdk 24
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        debug {
            debuggable true
        }
    }
}

dependencies {
    // INTENTIONALLY EMPTY
}
